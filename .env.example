GITHUB_TOKEN=

# Optional. Enables LLM-based ACID repo analysis (Architecture, Cross-Domain,
# Innovation, Documentation -- see src/buildscore/acid.py), via Groq's
# hosted API serving open-source models -- free tier available at
# console.groq.com, much cheaper than Claude/GPT even beyond that. Without
# this, `ambition` and `quality` still work, just from the older heuristics
# alone.
GROQ_API_KEY=
