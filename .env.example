GITHUB_TOKEN=

# Optional. Enables LLM-based ACID repo analysis (Architecture, Cross-Domain,
# Innovation, Documentation -- see src/buildscore/acid.py). Without this,
# `ambition` and `quality` still work, just from the older heuristics alone.
# This is the one part of the pipeline with a real per-scan cost.
ANTHROPIC_API_KEY=
